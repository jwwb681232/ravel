# Ravel environment — .env
# Copy this file to .env.local for local overrides (git-ignored).

APP_NAME=testblog
APP_ENV=local
APP_DEBUG=true
APP_URL=http://localhost:3000

DB_CONNECTION=sqlite
DB_DATABASE=database/ravel.sqlite
