// routes/web.rs — Web routes
//
// Register your web routes here.
//
// Example (once ravel-http is available):
//
//   use ravel_http::Route;
//   use crate::app::http::controllers::HomeController;
//
//   pub fn routes() -> Route {
//       Route::new()
//           .get("/", HomeController::index)
//           .get("/users/:id", HomeController::show)
//   }
//
pub fn routes() {
    // TODO: Return ravel_http::Route when available (Phase 4).
}
