# `database.toml` — every key is namespaced under `database.*`.
# SQLite by default; switch driver and point at real env values for
# other backends.

[default]
driver   = "sqlite"
database = { from = "DATABASE_NAME", default = "database.sqlite" }
